import { AbstractFieldConfig } from "../AbstractFieldConfig.js";

export class GenericFieldConfig extends AbstractFieldConfig {
  // No necesita lógica adicional por ahora.
  // Hereda render(), postRender(), listeners y data.
  // Es la belleza de la orientación a objetos.
}
